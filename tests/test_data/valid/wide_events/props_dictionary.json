{
    "appName": {
        "type": "string",
        "description": "Name of the application",
        "enum": ["testApp"]
    },
    "appVersion": {
        "type": "string",
        "description": "Version of the application",
        "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
        "examples": ["1.0.0", "1.0.10"]
    },
    "itemCountBucketed": {
        "type": "integer",
        "description": "Number of credentials imported, bucketed to preserve privacy",
        "enum": [0, 1, 2, 5, 10, 20, 50, 100, 200, 500, 1000]
    }
}
